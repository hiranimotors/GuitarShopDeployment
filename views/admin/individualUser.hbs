
<div class="container">
<div id="form" >
  <h2>{{name}}</h2>
  <form action="/admin/users/{{_id}}/edit" method="POST">

      
      <input name="email" value="{{email}}" class="form-control" required />
        <input type="text" name="name" value="{{name}}" class="form-control" required />

      <select id="consent" name="consent" class="form-control">
        <option selected>Does this user consent to receive marketing?</option>
        <option value="Yes" selected>Yes</option>
        <option value="No">No</option>
        </select>
    </label>
    <button type="submit" class="btn btn-primary">Edit Details</button>
                <form method="POST" action="/profile/delete">
<button class="btn btn-danger" type="submit">Delete User</button>
</form>

    {{#if errorMessage}}
      <p class="error">{{errorMessage}}</p>
    {{/if}}
    {{#if newUserMessage}}
      <p>{{newUserMessage}}</p>
    {{/if}}
  </form>
</div>
</div>